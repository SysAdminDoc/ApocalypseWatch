# Visual / UX

- P1 | client/src/styles/theme.css:20-22, client/src/styles/components.css:151-157 | Problem: `--text-tertiary` and card eyebrow/title usage are below a reliable WCAG AA contrast floor on the dark glass surfaces, so key dashboard labels read softer than comparable Linear/Vercel/Cloudflare operational dashboards. | Proposed fix: raise tertiary/muted token luminance and keep low-emphasis metadata at AA contrast.
- P1 | client/src/App.jsx:116-127 | Problem: stale `liveStatus.latestSampledAt` is not detected; a cached sample several hours old presents as ordinary live data unless `lastError` is also set. | Proposed fix: compute sample age against `liveStatus.cadenceMinutes` and show a warning banner once the sample is more than two cadences old.
- P1 | client/src/App.jsx:67-82, client/src/hooks/useDashboard.js:13-30 | Problem: background polling failures after the first successful payload are invisible because `error && data` is never surfaced in the UI. | Proposed fix: return retry metadata from `useDashboard` and show a non-fatal fetch warning banner while stale data remains on screen.
- P1 | client/src/styles/global.css:22-36, client/src/components/ArchiveChart.jsx:83-90, client/src/components/AboutCard.jsx:16-17 | Problem: keyboard focus state is not defined for links, range buttons, or `<summary>`, leaving keyboard users without a visible focus ring. | Proposed fix: add global `:focus-visible` styles and component-level offsets where needed.
- P1 | client/src/components/Hero.jsx:77, client/src/styles/global.css:194-200 | Problem: the hero aircraft uses SVG SMIL animation, which is not stopped by the CSS `prefers-reduced-motion` rule. | Proposed fix: replace SMIL with CSS keyframe animation on a class so the global reduced-motion rule disables it.
- P2 | client/index.html:2, client/src/styles/theme.css:81-96 | Problem: light theme exists only as a manual `[data-theme='light']` override while the document hardcodes `data-theme="dark"`, so dark-to-light theme parity and system preference behavior are incomplete. | Proposed fix: add a real theme resolver or explicit toggle, then audit map/chart/banner contrast in light mode.
- P2 | client/src/styles/global.css:95-108 | Problem: ultra-wide layout caps at 1480px, causing a single narrow dashboard column on 1920px+ displays instead of the denser, scan-friendly layouts used by Vercel Observability and Cloudflare Analytics. | Proposed fix: add a wider breakpoint with a larger shell max and a denser grid for map/chart/list panels.
- P2 | client/src/styles/components.css:40-45, client/src/styles/components.css:95-103 | Problem: hero and gauge typography use large viewport-scaled display sizing inside dashboard panels, making the app feel more like a landing page than a dense operational console. | Proposed fix: tighten panel type scale and reserve hero-scale text for the page heading only.
- P2 | client/src/App.jsx:72-77, client/src/components/Hero.jsx:19-20, client/src/components/EmergencyGauge.jsx:116-119, client/src/components/ArchiveChart.jsx:99-100 | Problem: inline presentation styles fragment the design system and make light theme parity harder to audit. | Proposed fix: move these styles to semantic classes in `components.css`.
- P2 | client/src/styles/components.css:211-222 | Problem: range tabs are visually pill buttons but do not support arrow-key tab behavior or associated tab panels. | Proposed fix: either implement full tablist keyboard semantics or switch to a segmented control with `aria-pressed`.

# Code Quality / Accessibility

- P1 | client/src/hooks/useDashboard.js:13-30 | Problem: `fetch` has no `AbortController`, no timeout, and no retry budget/backoff, so slow requests can overlap and transient outages keep polling at the fixed 60s cadence. | Proposed fix: replace `setInterval` with an abortable `setTimeout` loop using exponential backoff capped to a small retry budget.
- P1 | client/src/components/EmergencyGauge.jsx:12, client/src/components/EmergencyGauge.jsx:56-66, client/src/components/EmergencyGauge.jsx:90 | Problem: level colors are hardcoded as hex constants in the component instead of reading from the emergency accent token system. | Proposed fix: move segment colors to CSS variables/classes and keep active state tied to `var(--accent)` / emergency CSS tokens.
- P1 | client/src/components/GlobalMap.jsx:23-33 | Problem: map point normalization accepts only already-numeric coordinates, has no lat/lon range guard, and can project impossible values. | Proposed fix: coerce numeric inputs, reject malformed or out-of-range `lat`/`lon`, and count dropped points for copy.
- P1 | client/src/components/AircraftList.jsx:16-24 | Problem: sorting, mapping, and slicing the aircraft list runs on every parent render even when the aircraft array is unchanged. | Proposed fix: wrap the derived list in `useMemo`.
- P2 | client/src/components/AboutCard.jsx:21-23 | Problem: missing signal values render as `0`/`0.00σ`, which reads like confirmed baseline data instead of unavailable data. | Proposed fix: reuse `formatCount`/`formatSigma` and preserve unavailable values as `—`.
- P2 | client/src/components/StatusBanner.jsx:1-6 | Problem: banner content has no layout affordance for long backend error strings; a long ADS-B or Telegram error can dominate the first viewport. | Proposed fix: add wrapping, max-width, and optional detail disclosure for long operational errors.
- P2 | client/src/components/GlobalMap.jsx:61 | Problem: SVG map has a generic `aria-label` and no textual fallback for screen readers summarizing visible/dropped aircraft counts. | Proposed fix: add an adjacent `sr-only` summary and include invalid-coordinate drops.

# Performance

- P1 | client/src/App.jsx:5-6, client/src/components/GlobalMap.jsx:2-4, client/src/components/ArchiveChart.jsx:2-11 | Problem: `d3-geo`, `topojson-client`, `world-atlas`, and `recharts` are imported into the initial route, producing a 682.45 kB / 216.75 kB gzip bundle for a single-page dashboard. | Proposed fix: `React.lazy` split `GlobalMap` and `ArchiveChart` behind Suspense fallbacks so the initial bundle targets <100 kB gzip.
- P1 | client/src/components/ArchiveChart.jsx:65-72 | Problem: expanding the full 365-day archive occurs before range filtering; the memo helps, but the chart chunk still does the full decode whenever a new dashboard payload lands. | Proposed fix: keep decode inside the lazy chart chunk and add unsupported/empty archive guards before rendering Recharts.
- P2 | client/vite.config.js:11-13 | Problem: `chunkSizeWarningLimit: 1500` suppresses useful bundle regression warnings. | Proposed fix: lower the warning limit after lazy-loading heavy panels and document the intended chunk split.
- P2 | client/src/styles/global.css:115-124 | Problem: every card uses backdrop blur, which is visually rich but expensive on low-end mobile GPUs. | Proposed fix: reduce blur on mobile and add a `@supports` fallback for browsers without performant backdrop filtering.

# Robustness

- P1 | client/src/components/ArchiveChart.jsx:34, client/src/components/ArchiveChart.jsx:98-101 | Problem: `archive.v !== 1`, missing `t0`, or malformed RLE arrays silently collapse to "No samples in range yet", hiding a data-contract failure. | Proposed fix: distinguish empty archives from unsupported/malformed archives and show a warning state in the chart card.
- P1 | client/src/components/GlobalMap.jsx:23-46, client/src/components/GlobalMap.jsx:56-58 | Problem: malformed `lat`/`lon` records are silently dropped, so the aircraft list and map can disagree without explaining why. | Proposed fix: validate coordinates, track dropped records, and show the visible/hidden count in the map metadata.
- P1 | client/src/App.jsx:122-126 | Problem: `liveStatus.lastError` is shown, but the UI does not pair it with sample age or last-success context, making it unclear whether the data is still operationally fresh. | Proposed fix: add stale-sample detection and include next retry/refresh timing in status banners.
- P2 | server/telegram-alert.js:105-107, scripts/send_telegram_alert.js:60-62 | Problem: Telegram channel outages fail the alert script but are not represented in `/api/dashboard`, so the client cannot show alert-channel health. | Proposed fix: persist alert delivery status into the dashboard snapshot or expose it via the existing payload if the backend contract is later allowed to evolve.
- P2 | client/src/App.jsx:98-104 | Problem: `liveAircraft` and `cohort` are trusted as correctly shaped objects; malformed payloads can degrade into misleading counts instead of a guarded degraded state. | Proposed fix: add small client-side normalizers for array/object fields and display an invalid-payload banner when required sections are missing.
